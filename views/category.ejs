<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title%>></title>
    <link rel="stylesheet" href=/styles.css>
</head>
<body>
    <div class="body-container">
        <%- include("categories")%>
    
        <div class="main-container">
            <%- include("header"); -%>
            <div>
                <h1><%= title%></h1>
    <form action="/<%= category.id%>" method="POST">
        <button type="submit" onClick="return confirm('Are you sure you want to delete this category?')">Delete Category</button>
    </form>
    <a href="/<%=category.id%>/editCategory">Edit Category</a>
    <ul>
        <% if(locals.food){%>
            <% food.map((product) => {%>
                
                <li>
                    <a href="/food/<%=product.id%>"><%= product.food_name%></a>
                </li>
            <%})%>
        <%}%>
    </ul>
            </div>
        </div>
    </div>
    
</body>
</html>

